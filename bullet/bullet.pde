import traer.physics.*; // http://www.cs.princeton.edu/~traer/physics/

ParticleSystem physics;
Attraction a[];
Particle p[];

void setup() {
  frameRate(30);
  size(400, 400);
  noStroke();
  smooth();
  fill(255);
  
  physics = new ParticleSystem(0.01, 0.01)
  
  a[] = 
}
