// Fireflies globals
ArrayList vectorArray;

// Ring_trick_color globals
Ripple[] rip; // Declare the object array
int numRipples = 50; // Max number of ripples
int currentRipple = 0; // To keep track

// Color array
int c1 = 255;
int c2 = 127;
color[] c = { color(c1, c2, 0), color(c1, 0, c2), color(c2, c1, 0), color(c2, 0, c1), color(0, c2, c1) };

// MultiSketch globals
PApplet sketch;

void setup() {
  size(700, 400);
  sketch = new PApplet()
}

void draw() {
  sketch.draw()
}

void keyPressed() {
  if (keyCode == ' ') {
    if () {
      
    }
  }
}